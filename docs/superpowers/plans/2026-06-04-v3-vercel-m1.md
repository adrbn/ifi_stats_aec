# v3 sur Vercel — M1 (en ligne + auth + historique + comparateur) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mettre la v3 en ligne sur Vercel (front Next.js + backend FastAPI serverless), protégée par un mot de passe partagé, servant les données historiques, avec le comparateur v2/v3 intégré.

**Architecture:** Un projet Vercel, Root Directory = `oscar-prealpha/web`. Le backend FastAPI est déplacé sous `web/api/` pour être bundlé par la convention Vercel `/api/*.py` (aucun `vercel.json` requis pour les fonctions). Un middleware Next.js protège pages + `/api/*` via un cookie de session signé. La v2 reste sur Streamlit Cloud, embarquée en iframe dans une route `/compare`.

**Tech Stack:** Next.js 14 (App Router), FastAPI sur runtime Python Vercel, pandas, `jose` (JWT cookie côté Next), Vitest (tests front), pytest (tests back).

**Référence spec :** `docs/superpowers/specs/2026-06-04-v3-deploy-vercel-design.md`

---

## Structure des fichiers (verrouillage des décisions)

Restructuration pour que Vercel bundle le Python (Root Directory = `oscar-prealpha/web`) :

```
oscar-prealpha/web/
├── api/                      ← fonctions serverless Python (bundlées par Vercel)
│   ├── index.py              ← NOUVEAU : entrée ASGI (expose l'app FastAPI sur /api/*)
│   ├── main.py               ← DÉPLACÉ depuis ../api (startup allégé pour serverless)
│   ├── engine.py             ← DÉPLACÉ depuis ../api
│   ├── oscar_core.py         ← DÉPLACÉ depuis ../api
│   ├── aec_parser_v3.py      ← DÉPLACÉ depuis ../api
│   ├── build_snapshot.py     ← DÉPLACÉ (+ build_profils.py, build_produits.py)
│   ├── requirements.txt      ← DÉPLACÉ
│   ├── fixtures/*.json        ← DÉPLACÉ (snapshot/profils/produits)
│   └── data/                  ← COPIE des .xlsx/.zip/.csv (pour engine.compute en prod)
├── app/
│   ├── login/page.tsx        ← NOUVEAU : page mot de passe
│   ├── compare/page.tsx      ← NOUVEAU : comparateur v2/v3 (porte la coque)
│   └── api/auth/
│       ├── login/route.ts    ← NOUVEAU : vérifie le mot de passe, pose le cookie
│       └── logout/route.ts   ← NOUVEAU : efface le cookie
├── lib/auth.ts               ← NOUVEAU : signe/vérifie le cookie de session (jose)
├── middleware.ts             ← NOUVEAU : protège pages + /api/*
├── next.config.mjs           ← MODIFIÉ : rewrites /api seulement en dev local
└── .env.local.example        ← NOUVEAU : doc des variables d'env
```

> **Note dev local :** après déplacement, le backend se lance désormais depuis
> `oscar-prealpha/web/api` (`uvicorn index:app --port 8000`). Le `start-compare.sh` et
> `api/run.sh` sont mis à jour en conséquence (Task 9).

---

## Task 1 : Déplacer le backend Python sous `web/api/`

**Files:**
- Move: `oscar-prealpha/api/*` → `oscar-prealpha/web/api/`
- Copy: `oscar-prealpha/../data/` (racine repo `data/`) → `oscar-prealpha/web/api/data/`

- [ ] **Step 1 : Déplacer les modules Python et fixtures**

```bash
cd oscar-prealpha
git mv api/main.py api/engine.py api/oscar_core.py api/aec_parser_v3.py \
       api/build_snapshot.py api/build_profils.py api/build_produits.py \
       api/requirements.txt api/run.sh web/api/ 2>/dev/null || {
  mkdir -p web/api && git mv api/* web/api/ ; }
git mv api/fixtures web/api/fixtures
```

- [ ] **Step 2 : Copier les données pour `engine.compute` en prod**

`engine.py` lit les exports depuis `data/`. Pour que le calcul fonctionne dans la
fonction serverless, les données doivent être sous le Root Directory.

```bash
cd oscar-prealpha
cp -R ../data web/api/data
```

- [ ] **Step 3 : Adapter le chemin data dans engine.py si besoin**

Ouvrir `web/api/engine.py`, repérer la constante de chemin vers les données (ex.
`os.path.join(HERE, "..", "..", "data")`). La faire pointer d'abord sur le data
bundlé, avec repli sur la racine repo (dev) :

```python
import os
HERE = os.path.dirname(os.path.abspath(__file__))
# Prod (Vercel) : data bundlé sous web/api/data ; dev : repli sur data/ du repo.
DATA_DIR = os.path.join(HERE, "data")
if not os.path.isdir(DATA_DIR):
    DATA_DIR = os.path.join(HERE, "..", "..", "..", "data")
```

- [ ] **Step 4 : Ignorer le data copié dans git (éviter le doublon de 5,6 Mo)**

```bash
cd oscar-prealpha/web/api
printf "data/\n" >> .gitignore
```

> Le data est copié au build par une étape Vercel (Task 8). En dev local, `engine.py`
> retombe sur le `data/` du repo. On ne committe pas le doublon.

- [ ] **Step 5 : Commit**

```bash
cd /Users/adrien/vibecoding/claudecode_repos/ifi/stats_aec_app
git add -A && git commit -m "refactor: move v3 backend under web/api for Vercel bundling"
```

---

## Task 2 : Alléger le startup FastAPI pour le serverless

**Files:**
- Modify: `oscar-prealpha/web/api/main.py:57-68`
- Test: `oscar-prealpha/web/api/tests/test_startup.py`

Le `@app.on_event("startup")` recompute tout le snapshot à chaque cold start (lent).
En serverless on veut un démarrage léger : charger la fixture (rapide), lazy-load engine.

- [ ] **Step 1 : Écrire le test qui échoue**

```python
# oscar-prealpha/web/api/tests/test_startup.py
import importlib

def test_startup_loads_fixture_without_recompute(monkeypatch):
    called = {"build": False}
    import build_snapshot
    monkeypatch.setattr(build_snapshot, "build", lambda: (_ for _ in ()).throw(AssertionError("should not recompute")))
    import main
    main._startup()
    assert main.SNAPSHOT.get("meta", {}).get("app") == "OSCAR"
    assert called["build"] is False
```

- [ ] **Step 2 : Lancer le test (échoue)**

Run: `cd oscar-prealpha/web/api && python -m pytest tests/test_startup.py -v`
Expected: FAIL (le startup actuel appelle `build_snapshot.build()`).

- [ ] **Step 3 : Modifier le startup (fixture d'abord, engine lazy)**

Remplacer `_load_snapshot` + `_startup` dans `main.py` :

```python
def _load_snapshot() -> dict:
    """Serverless: charge la fixture pré-calculée (rapide). engine lazy à la requête."""
    return _load_snapshot_from_disk()


@app.on_event("startup")
def _startup() -> None:
    global SNAPSHOT
    SNAPSHOT = _load_snapshot()
```

- [ ] **Step 4 : Lancer le test (passe)**

Run: `cd oscar-prealpha/web/api && python -m pytest tests/test_startup.py -v`
Expected: PASS

- [ ] **Step 5 : Commit**

```bash
git add oscar-prealpha/web/api/main.py oscar-prealpha/web/api/tests/test_startup.py
git commit -m "perf: lighten FastAPI startup for serverless (fixture load, lazy engine)"
```

---

## Task 3 : Entrée ASGI serverless `web/api/index.py`

**Files:**
- Create: `oscar-prealpha/web/api/index.py`

Vercel route `/api/*` vers `api/index.py`. FastAPI gère déjà les chemins `/api/...`.

- [ ] **Step 1 : Créer l'entrée**

```python
# oscar-prealpha/web/api/index.py
"""Point d'entrée serverless Vercel : expose l'app FastAPI (routes /api/*)."""
from main import app  # noqa: F401  (Vercel détecte `app` comme handler ASGI)
```

- [ ] **Step 2 : Vérifier en local que l'app s'importe et répond**

Run:
```bash
cd oscar-prealpha/web/api
python -c "from index import app; print([r.path for r in app.routes if hasattr(r,'path')])"
```
Expected: la liste contient `/api/health`, `/api/cours`, `/api/snapshot`, `/api/meta`.

- [ ] **Step 3 : Commit**

```bash
git add oscar-prealpha/web/api/index.py
git commit -m "feat: add Vercel ASGI entrypoint for FastAPI backend"
```

---

## Task 4 : Front en same-origin `/api` (prod) + rewrite dev

**Files:**
- Modify: `oscar-prealpha/web/next.config.mjs`

- [ ] **Step 1 : Limiter le rewrite localhost au dev uniquement**

```javascript
/** @type {import('next').NextConfig} */
const isDev = process.env.NODE_ENV !== "production";
const API = process.env.OSCAR_API_URL || "http://localhost:8000";

const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["three"],
  async rewrites() {
    // En prod sur Vercel, /api est servi par la fonction Python (même origine) :
    // aucun rewrite. En dev local, on proxifie vers uvicorn :8000.
    return isDev ? [{ source: "/api/:path*", destination: `${API}/api/:path*` }] : [];
  },
};

export default nextConfig;
```

- [ ] **Step 2 : Vérifier le build Next**

Run: `cd oscar-prealpha/web && npm run build`
Expected: build réussi, aucune erreur de config.

- [ ] **Step 3 : Commit**

```bash
git add oscar-prealpha/web/next.config.mjs
git commit -m "chore: same-origin /api in prod, localhost rewrite in dev only"
```

---

## Task 5 : Logique d'auth (cookie signé) — `lib/auth.ts`

**Files:**
- Create: `oscar-prealpha/web/lib/auth.ts`
- Create: `oscar-prealpha/web/lib/auth.test.ts`
- Modify: `oscar-prealpha/web/package.json` (dépendances `jose`, `vitest`)

- [ ] **Step 1 : Installer les dépendances**

```bash
cd oscar-prealpha/web
npm install jose
npm install -D vitest
```

- [ ] **Step 2 : Écrire le test qui échoue**

```typescript
// oscar-prealpha/web/lib/auth.test.ts
import { describe, it, expect } from "vitest";
import { createSession, verifySession } from "./auth";

const SECRET = "test-secret-at-least-32-chars-long-xxxx";

describe("auth session", () => {
  it("crée un cookie vérifiable", async () => {
    const token = await createSession(SECRET);
    expect(await verifySession(token, SECRET)).toBe(true);
  });
  it("rejette un token altéré", async () => {
    const token = await createSession(SECRET);
    expect(await verifySession(token + "x", SECRET)).toBe(false);
  });
  it("rejette un token vide", async () => {
    expect(await verifySession("", SECRET)).toBe(false);
  });
});
```

- [ ] **Step 3 : Lancer le test (échoue)**

Run: `cd oscar-prealpha/web && npx vitest run lib/auth.test.ts`
Expected: FAIL (`createSession` non défini).

- [ ] **Step 4 : Implémenter `lib/auth.ts`**

```typescript
// oscar-prealpha/web/lib/auth.ts
import { SignJWT, jwtVerify } from "jose";

const ALG = "HS256";
export const SESSION_COOKIE = "oscar_session";

function key(secret: string): Uint8Array {
  return new TextEncoder().encode(secret);
}

/** Crée un JWT de session valable 7 jours. */
export async function createSession(secret: string): Promise<string> {
  return await new SignJWT({ ok: true })
    .setProtectedHeader({ alg: ALG })
    .setIssuedAt()
    .setExpirationTime("7d")
    .sign(key(secret));
}

/** Vérifie la signature + l'expiration. Ne lève jamais. */
export async function verifySession(token: string, secret: string): Promise<boolean> {
  if (!token) return false;
  try {
    await jwtVerify(token, key(secret), { algorithms: [ALG] });
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 5 : Lancer le test (passe)**

Run: `cd oscar-prealpha/web && npx vitest run lib/auth.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 6 : Commit**

```bash
git add oscar-prealpha/web/lib/auth.ts oscar-prealpha/web/lib/auth.test.ts oscar-prealpha/web/package.json oscar-prealpha/web/package-lock.json
git commit -m "feat: signed session cookie helpers (jose) with tests"
```

---

## Task 6 : Routes d'auth (login / logout)

**Files:**
- Create: `oscar-prealpha/web/app/api/auth/login/route.ts`
- Create: `oscar-prealpha/web/app/api/auth/logout/route.ts`

Le mot de passe partagé est comparé à un hash SHA-256 stocké en env
`OSCAR_PASSWORD_SHA256`. Le secret de signature est `OSCAR_SESSION_SECRET`.

- [ ] **Step 1 : Route login**

```typescript
// oscar-prealpha/web/app/api/auth/login/route.ts
import { NextResponse } from "next/server";
import { createSession, SESSION_COOKIE } from "@/lib/auth";

async function sha256Hex(s: string): Promise<string> {
  const buf = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(s));
  return Array.from(new Uint8Array(buf)).map((b) => b.toString(16).padStart(2, "0")).join("");
}

export async function POST(req: Request) {
  const { password } = await req.json().catch(() => ({ password: "" }));
  const expected = process.env.OSCAR_PASSWORD_SHA256 ?? "";
  const secret = process.env.OSCAR_SESSION_SECRET ?? "";
  if (!expected || !secret) {
    return NextResponse.json({ error: "auth non configurée" }, { status: 500 });
  }
  if ((await sha256Hex(String(password))) !== expected) {
    return NextResponse.json({ error: "mot de passe incorrect" }, { status: 401 });
  }
  const token = await createSession(secret);
  const res = NextResponse.json({ ok: true });
  res.cookies.set(SESSION_COOKIE, token, {
    httpOnly: true, secure: true, sameSite: "lax", path: "/", maxAge: 60 * 60 * 24 * 7,
  });
  return res;
}
```

- [ ] **Step 2 : Route logout**

```typescript
// oscar-prealpha/web/app/api/auth/logout/route.ts
import { NextResponse } from "next/server";
import { SESSION_COOKIE } from "@/lib/auth";

export async function POST() {
  const res = NextResponse.json({ ok: true });
  res.cookies.set(SESSION_COOKIE, "", { path: "/", maxAge: 0 });
  return res;
}
```

- [ ] **Step 3 : Vérifier le build**

Run: `cd oscar-prealpha/web && npm run build`
Expected: build réussi (routes `/api/auth/login`, `/api/auth/logout` détectées).

- [ ] **Step 4 : Commit**

```bash
git add oscar-prealpha/web/app/api/auth
git commit -m "feat: shared-password login/logout routes"
```

---

## Task 7 : Middleware de protection (pages + /api/*)

**Files:**
- Create: `oscar-prealpha/web/middleware.ts`

- [ ] **Step 1 : Créer le middleware**

```typescript
// oscar-prealpha/web/middleware.ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { verifySession, SESSION_COOKIE } from "@/lib/auth";

// Chemins accessibles sans session.
const PUBLIC = ["/login", "/api/auth/login"];

export async function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl;
  if (PUBLIC.some((p) => pathname === p || pathname.startsWith(p + "/"))) {
    return NextResponse.next();
  }
  const token = req.cookies.get(SESSION_COOKIE)?.value ?? "";
  const ok = await verifySession(token, process.env.OSCAR_SESSION_SECRET ?? "");
  if (ok) return NextResponse.next();

  // API protégée → 401 JSON ; pages → redirection login.
  if (pathname.startsWith("/api/")) {
    return NextResponse.json({ error: "non autorisé" }, { status: 401 });
  }
  const url = req.nextUrl.clone();
  url.pathname = "/login";
  return NextResponse.redirect(url);
}

// On exclut les assets statiques Next.
export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 2 : Vérifier le build**

Run: `cd oscar-prealpha/web && npm run build`
Expected: build réussi, middleware compilé.

- [ ] **Step 3 : Commit**

```bash
git add oscar-prealpha/web/middleware.ts
git commit -m "feat: auth middleware protecting pages and /api/*"
```

---

## Task 8 : Page de login

**Files:**
- Create: `oscar-prealpha/web/app/login/page.tsx`

- [ ] **Step 1 : Créer la page**

```tsx
// oscar-prealpha/web/app/login/page.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export default function LoginPage() {
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [busy, setBusy] = useState(false);
  const router = useRouter();

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setBusy(true); setError("");
    const res = await fetch("/api/auth/login", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ password }),
    });
    setBusy(false);
    if (res.ok) router.replace("/");
    else setError("Mot de passe incorrect.");
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-neutral-50 px-4">
      <form onSubmit={submit} className="w-full max-w-sm rounded-xl border border-neutral-200 bg-white p-8 shadow-sm">
        <h1 className="mb-1 text-lg font-bold tracking-[0.12em]">OSCAR</h1>
        <p className="mb-6 text-sm text-neutral-500">Accès réservé au personnel autorisé</p>
        <label className="mb-2 block text-sm font-medium" htmlFor="pwd">Mot de passe</label>
        <input
          id="pwd" type="password" value={password} autoFocus
          onChange={(e) => setPassword(e.target.value)}
          className="mb-4 w-full rounded-md border border-neutral-300 px-3 py-2 text-sm outline-none focus:border-accent-600"
        />
        {error && <p className="mb-4 text-sm text-red-600">{error}</p>}
        <button
          type="submit" disabled={busy}
          className="w-full rounded-md bg-neutral-900 px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-neutral-800 disabled:opacity-60"
        >
          {busy ? "Connexion…" : "Se connecter"}
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Step 2 : Test local de bout en bout (auth)**

Run (dans deux terminaux) :
```bash
# terminal A — backend
cd oscar-prealpha/web/api && uvicorn index:app --port 8000
# terminal B — front, avec env de test
cd oscar-prealpha/web
export OSCAR_SESSION_SECRET="dev-secret-at-least-32-characters-xx"
export OSCAR_PASSWORD_SHA256="$(printf 'motdepasse' | shasum -a 256 | cut -d' ' -f1)"
npm run dev
```
Puis : ouvrir `http://localhost:3000` → redirige vers `/login` ; entrer `motdepasse`
→ accès au dashboard. Vérifier qu'un `curl http://localhost:3000/api/health` sans
cookie renvoie 401.

Expected: redirection login OK, login OK, `/api/*` protégé.

- [ ] **Step 3 : Commit**

```bash
git add oscar-prealpha/web/app/login/page.tsx
git commit -m "feat: login page (shared password)"
```

---

## Task 9 : Comparateur en route Next `/compare` + maj dev scripts

**Files:**
- Create: `oscar-prealpha/web/app/compare/page.tsx`
- Modify: `oscar-prealpha/start-compare.sh` (chemin api), `oscar-prealpha/web/api/run.sh`

- [ ] **Step 1 : Porter la coque en composant React**

Reprendre la logique de `oscar-prealpha/shell/index.html` (toggle + 2 iframes
persistantes). v3 = même origine (`/cours/synthese`), v2 = iframe Streamlit live.

```tsx
// oscar-prealpha/web/app/compare/page.tsx
"use client";
import { useEffect, useState } from "react";

const V2_URL = "https://ifi-stats-aec.streamlit.app/?embed=true";
const V3_URL = "/cours/synthese";

export default function ComparePage() {
  const [v, setV] = useState<"v2" | "v3">("v2");
  useEffect(() => {
    const h = (e: KeyboardEvent) => {
      if (e.key === "1") setV("v2");
      else if (e.key === "2") setV("v3");
    };
    addEventListener("keydown", h);
    return () => removeEventListener("keydown", h);
  }, []);
  return (
    <div className="flex h-screen flex-col">
      <div className="flex h-14 items-center justify-between border-b border-neutral-200 bg-white px-4">
        <span className="text-sm font-bold tracking-[0.14em]">OSCAR · comparateur</span>
        <div className="relative inline-flex rounded-lg bg-neutral-100 p-1">
          <span
            className="absolute top-1 h-[calc(100%-8px)] w-[calc(50%-4px)] rounded-md bg-white shadow transition-transform duration-200"
            style={{ transform: v === "v3" ? "translateX(100%)" : "translateX(0)" }}
          />
          <button onClick={() => setV("v2")} className={`relative z-10 px-4 py-1.5 text-sm font-semibold ${v === "v2" ? "text-neutral-900" : "text-neutral-500"}`}>v2 · Streamlit</button>
          <button onClick={() => setV("v3")} className={`relative z-10 px-4 py-1.5 text-sm font-semibold ${v === "v3" ? "text-neutral-900" : "text-neutral-500"}`}>v3 · Nouvelle UI</button>
        </div>
        <span className="text-xs text-neutral-500">{v === "v2" ? "en ligne · production" : "natif · Vercel"}</span>
      </div>
      <div className="relative flex-1">
        <iframe src={V2_URL} title="v2" className={`absolute inset-0 h-full w-full border-0 ${v === "v2" ? "" : "invisible"}`} />
        <iframe src={V3_URL} title="v3" className={`absolute inset-0 h-full w-full border-0 ${v === "v3" ? "" : "invisible"}`} />
      </div>
    </div>
  );
}
```

> Les deux iframes restent montées (`invisible` ≠ démontage) → état conservé au switch.

- [ ] **Step 2 : Mettre à jour les scripts de dev (chemin api déplacé)**

Dans `oscar-prealpha/start-compare.sh`, remplacer `cd "$ROOT/api"` par
`cd "$ROOT/web/api"` et la commande de lancement par `uvicorn index:app --reload --port 8000`.
Mettre à jour `oscar-prealpha/web/api/run.sh` de même (entrée `index:app`).

- [ ] **Step 3 : Vérifier le build + la route**

Run: `cd oscar-prealpha/web && npm run build`
Expected: build réussi, route `/compare` présente dans la sortie.

- [ ] **Step 4 : Commit**

```bash
git add oscar-prealpha/web/app/compare/page.tsx oscar-prealpha/start-compare.sh oscar-prealpha/web/api/run.sh
git commit -m "feat: comparator as native /compare route; fix dev scripts for moved api"
```

---

## Task 10 : Étape de build Vercel (copier data au build)

**Files:**
- Create: `oscar-prealpha/web/vercel-build.sh`
- Modify: `oscar-prealpha/web/package.json` (script `vercel-build`)
- Create: `oscar-prealpha/web/.env.local.example`

Le `data/` n'est pas committé (Task 1). Il faut le matérialiser dans le bundle au build
Vercel (le repo entier est checkout par Vercel, donc `../../../data` existe au build).

- [ ] **Step 1 : Script de build**

```bash
# oscar-prealpha/web/vercel-build.sh
#!/usr/bin/env bash
set -euo pipefail
# Copier les données du repo dans le dossier bundlé par la fonction Python.
if [ -d "../../data" ]; then
  mkdir -p api/data && cp -R ../../data/. api/data/
fi
next build
```

- [ ] **Step 2 : Brancher le script**

Dans `oscar-prealpha/web/package.json`, ajouter :
```json
"scripts": {
  "vercel-build": "bash vercel-build.sh"
}
```
(Vercel exécute `vercel-build` s'il existe, à la place de `build`.)

- [ ] **Step 3 : Documenter les variables d'env**

```bash
# oscar-prealpha/web/.env.local.example
# Hash SHA-256 du mot de passe partagé. Générer :
#   printf 'TON_MOT_DE_PASSE' | shasum -a 256 | cut -d' ' -f1
OSCAR_PASSWORD_SHA256=
# Secret de signature du cookie (≥32 caractères aléatoires). Générer :
#   openssl rand -hex 32
OSCAR_SESSION_SECRET=
```

- [ ] **Step 4 : Vérifier que le script tourne en local**

Run: `cd oscar-prealpha/web && chmod +x vercel-build.sh && bash vercel-build.sh`
Expected: `api/data/` peuplé, `next build` réussi.

- [ ] **Step 5 : Commit**

```bash
git add oscar-prealpha/web/vercel-build.sh oscar-prealpha/web/package.json oscar-prealpha/web/.env.local.example
git commit -m "build: copy data into python bundle on vercel build; document env"
git push origin main
```

---

## Task 11 : Déploiement Vercel (actions utilisateur + vérification)

**Files:** aucun (configuration sur vercel.com)

- [ ] **Step 1 : Créer le projet Vercel** *(utilisateur)*

Sur vercel.com (compte gratuit) → New Project → importer `adrbn/ifi_stats_aec` →
**Root Directory = `oscar-prealpha/web`** → Framework: Next.js (auto).

- [ ] **Step 2 : Variables d'env** *(utilisateur)*

Dans Project Settings → Environment Variables, ajouter pour *Production* :
- `OSCAR_PASSWORD_SHA256` = sortie de `printf 'LE_MOT_DE_PASSE' | shasum -a 256 | cut -d' ' -f1`
- `OSCAR_SESSION_SECRET` = sortie de `openssl rand -hex 32`

- [ ] **Step 3 : Ignored Build Step** *(utilisateur)*

Settings → Git → Ignored Build Step → commande :
```bash
git diff --quiet HEAD^ HEAD -- ../ ':(exclude)../../*' || exit 1
```
(But : ne rebuild que si `oscar-prealpha/` a changé. Si la commande sort 0 = skip.)
*Validation manuelle ; ajuster si le comportement diffère.*

- [ ] **Step 4 : Déployer puis vérifier**

Déclencher le déploiement. Une fois en ligne, vérifier :
- `https://<projet>.vercel.app/` → redirige vers `/login`.
- login avec le mot de passe → dashboard v3 s'affiche avec **données historiques**.
- `https://<projet>.vercel.app/api/health` sans session → 401.
- après login, le réseau montre `/api/cours` qui répond (200) avec des données.
- `https://<projet>.vercel.app/compare` → toggle v2/v3, les deux s'affichent.

Expected: les 5 points passent.

- [ ] **Step 5 : Point de validation du risque (taille bundle / durée)**

Si le déploiement échoue sur la taille de la fonction Python (>250 Mo) ou un timeout :
- vérifier les deps dans `web/api/requirements.txt` (épingler des versions légères) ;
- au besoin, basculer `/api/cours` sur le **filtrage de la fixture** (déjà en place via
  le fallback `except → SNAPSHOT`) en n'embarquant PAS `data/` (retirer Task 10 step 1)
  → bundle minimal, filtres limités à `byAntenna`/`evolution` (acceptable pour M1).

---

## Self-Review (couverture spec)

- §3 Architecture (Vercel, Next + FastAPI serverless, même origine) → Tasks 1,3,4,11 ✅
- §4 Données (snapshot pré-calculé, pandas non bloquant) → Tasks 2,10 ✅
- §6 Auth (mot de passe partagé, cookie, middleware pages+API) → Tasks 5,6,7,8 ✅
- §7 Comparateur derrière auth → Tasks 7,9 (route /compare couverte par le middleware) ✅
- §8 Config (vercel, env, same-origin) → Tasks 4,10,11 ✅
- §9 Jalon M1 → l'ensemble ✅ ; **Upload (M2)** = hors de ce plan (plan dédié suivant)
- §10 Risques (bundle, durée) → Task 11 step 5 ✅

**Cohérence des types/noms :** `SESSION_COOKIE`, `createSession`, `verifySession`
utilisés de façon identique entre `lib/auth.ts` (Task 5), routes (Task 6) et middleware
(Task 7) ✅. Env vars `OSCAR_PASSWORD_SHA256` / `OSCAR_SESSION_SECRET` cohérentes entre
Tasks 6, 8, 10, 11 ✅.

**Hors scope explicite :** upload (M2), persistance fichiers (M3), parité/retrait v2 (M4),
auth fine (post-M1).
