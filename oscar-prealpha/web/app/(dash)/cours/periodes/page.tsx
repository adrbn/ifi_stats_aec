import { ComingSoon } from "@/components/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      group="Cours"
      title="Par période"
      note="Analyse des cours par période (ANNÉE-MOIS, calculée sur la date de début réelle du cours)."
    />
  );
}
